# =============================================================================
# Staging Config — 스테이징 환경
# =============================================================================

# Explicit 모드: 시작 시 현재 저장소를 로드하고 이후 변경은 API로만 제어
--model-control-mode=explicit
--load-model=*

# Cache 활성화 (운영 전 검증용)
--cache-config=local,size=33554432

# 로깅 레벨 조정
--log-verbose=1
