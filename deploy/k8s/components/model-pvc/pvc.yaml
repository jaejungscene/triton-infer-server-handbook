apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: triton-model-repository
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  # storageClassName: nfs-client
