apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base
  - namespace.yaml
  - network_policy.yaml
  - hpa.yaml
  - pdb.yaml

patches:
  - path: replica_patch.yaml
  - path: resource_patch.yaml
  - path: triton_args_patch.yaml
