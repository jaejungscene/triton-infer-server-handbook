{
  "_metadata": {
    "latency_unit": "milliseconds",
    "source_latency_unit": "microseconds",
    "triton_image": "nvcr.io/nvidia/tritonserver:24.08-py3@sha256:0b6a7449ea6a46fd167376d40418b169ed82cd36addc6f6c56fe2cbda4294e13",
    "profile": "tests/perf/profiles.json",
    "note": "Example floors. Recalibrate with these versioned workloads on the dedicated runner before production enforcement."
  },
  "models": {
    "text_classifier": {
      "min_throughput": 50,
      "max_p95_latency_ms": 100,
      "concurrency": 8,
      "notes": "Python backend smoke model; conservative CI floor"
    },
    "resnet50": {
      "min_throughput": 100,
      "max_p95_latency_ms": 50,
      "concurrency": 8,
      "notes": "ONNX Runtime on A100, batch_size=16"
    },
    "yolox": {
      "min_throughput": 60,
      "max_p95_latency_ms": 100,
      "concurrency": 4,
      "notes": "TensorRT FP16 on A100, batch_size=8"
    },
    "od_pipeline": {
      "min_throughput": 40,
      "max_p95_latency_ms": 150,
      "concurrency": 4,
      "notes": "Ensemble (preprocess + yolox + postprocess)"
    },
    "anomaly_detector": {
      "min_throughput": 1000,
      "max_p95_latency_ms": 5,
      "concurrency": 8,
      "notes": "FIL backend, XGBoost, batch_size=4096"
    }
  }
}
